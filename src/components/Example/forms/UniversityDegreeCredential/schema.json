{
    "$id": "university-degree-vc-request-schema",
    "type": "object",
    "title": "Comment",
    "properties": {
        "issuer": {
            "title": "Issuer",
            "type": "string",
            "default": "did:web:vc.transmute.world"
        },
        "credentialSubjectId": {
            "title": "Credential Subject ID",
            "type": "string",
            "default": "did:example:123"
        }
    },
    "required": [
        "issuer",
        "credentialSubjectId"
    ]
}